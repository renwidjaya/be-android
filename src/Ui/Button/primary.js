import {Display} from '../../Utils';

const primary = () => ({
  buttonStyle: {
    height: 47,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 8,
    width: Display.setWidth(90),
  },
  buttonTextStyle: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: 'bold',
  },
});

export default primary;
